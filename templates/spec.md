# Spec — NNN-<kebab-name>

> Copy to `.specs/NNN-<kebab-name>/spec.md`. Replace `<PLACEHOLDERS>`. Keep stack-free.

## Summary

`<One or two sentences: what this feature enables, for whom.>`

## User story

As a `<role>`, I want `<capability>`, so that `<outcome>`.

(Additional stories below, one per sub-role if needed.)

## Acceptance criteria

1. `<Observable behavior, single sentence, declarative.>`
2. `<…>`
3. `<…>`

(Keep criteria atomic. If you can split with "and", split.)

## Non-goals

- `<What this feature does NOT cover; reserve for future.>`
- `<…>`

## Applicable constitution articles

- Article `<N>` — `<reason it applies>`
- Article `<N>` — `<…>`

## Open questions

- [NEEDS CLARIFICATION: `<question or option list>`]
- [NEEDS CLARIFICATION: `<…>`]

## Glossary additions (optional)

- `<Term>` — `<Definition as used in this spec>`.

---

## Closed (filled during verify)

- Date: `<YYYY-MM-DD>`
- Commit: `<sha>`
- Notes: `<non-obvious context for future readers>`
